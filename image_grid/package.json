{
  "name": "image-grid",
  "version": "1.0.0",
  "private": true,
  "description": "Lay images out on an A4 grid and export a print-ready PDF",
  "scripts": {
    "build": "node build.js",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jspdf": "^4.2.1",
    "sortablejs": "^1.15.6"
  },
  "type": "module"
}
